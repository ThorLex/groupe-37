import { Audiowide, Open_Sans } from "next/font/google";

export const Audiowided = Audiowide({
    subsets: ['latin'],
    weight: ['400'],
    variable: "--font-audiowided"
})

export const OpenSans = Open_Sans({
    subsets: ['latin'],
    variable: "--font-opensans"
})

